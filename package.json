{
  "name": "astro-sheet-loader-project",
  "workspaces": [
    "demo",
    "packages/*"
  ],
  "scripts": {
    "test": "cd demo && astro check && astro build && cd .. && vitest",
    "format": "prettier --write .",
    "dev": "cd demo && astro dev",
    "build": "cd demo && astro build",
    "preview": "cd demo && astro preview"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "prettier-plugin-astro": "^0.14.1"
  }
}
